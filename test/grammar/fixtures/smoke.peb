<div class="card" data-signals="{count: {{ initial }}}" hx-get="/api/{{ id }}">
  {# comment #}
  {% if user.isActive %}
    <p>Welcome, {{ user.name | upper }}!</p>
  {% endif %}
  <div>A</div>
</div>
